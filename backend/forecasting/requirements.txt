# torch is also installed via the CPU-only wheel index in the Dockerfile (smaller image).
torch>=2.2
numpy>=1.26
scikit-learn>=1.3
joblib>=1.3
fastapi>=0.110
uvicorn[standard]>=0.27
requests>=2.31
python-dotenv>=1.0
pydantic>=2.5
# Offline real-data training path (train.py -> data_loader.load_training_sequences) reads
# the twin's persisted history from TimescaleDB. Serving (main.py) never needs it, so the
# import is lazy and the loader degrades to synthetic data when it is absent.
psycopg2-binary>=2.9
