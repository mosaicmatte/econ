# Copy to .env and fill in. NEVER commit the real .env.
# Free key from https://openweathermap.org/api  (the service still runs without one,
# using a fallback outdoor temp/humidity flagged as weather_source="fallback").
OPENWEATHER_API_KEY=
WEATHER_LAT=10.7724
WEATHER_LON=106.6581
