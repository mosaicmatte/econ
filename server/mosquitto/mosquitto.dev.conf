# ANONYMOUS. Local demo only — no hardware, no building, no network you do not own.
#
# Mount this instead of mosquitto.conf when you want the old zero-setup behaviour:
#   volumes:
#     - ./mosquitto/mosquitto.dev.conf:/mosquitto/config/mosquitto.conf
#
# If a real relay is wired to a real circuit, you want mosquitto.conf.
listener 1883
allow_anonymous true
